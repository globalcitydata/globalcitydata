import React from 'react';
import Layout from '../components/layout';

export default () => (
  <Layout>
    <h1>Hi! I'm the last iteration of Global City Data!</h1>
    <p>I cannot wait to be done with this! Yay!</p>
  </Layout>
);
